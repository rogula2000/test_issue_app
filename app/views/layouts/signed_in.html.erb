<!DOCTYPE html>
<html>
  <head>
    <title>Issues App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body>
    <header class="bg-white border-b">
      <div class="container mx-auto px-6 py-3 flex items-center justify-between relative">
        <div class="">
          <%= link_to "Issues App", root_path, class: "py-2.5 px-6 text-sm font-semibold text-indigo-500 transition-all duration-500 hover:text-indigo-700" %>
        </div>
        <div class="right-0 rounded-md shadow-lg bg-white ring-1 ring-black/5">
          <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "py-2.5 px-6 text-sm bg-indigo-500 text-white rounded-lg cursor-pointer font-semibold text-center shadow-xs transition-all duration-500 hover:bg-indigo-700" %>
        </div>
      </div>
    </header>
    <div class="container mx-auto p-6">
      <% if notice %>
        <div class="bg-green-100 text-green-800 px-4 py-2 rounded mb-4"><%= notice %></div>
      <% end %>
      <% if alert %>
        <div class="bg-red-100 text-red-800 px-4 py-2 rounded mb-4"><%= alert %></div>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
